# MyCites MVP

## Frontend
- `cd frontend`
- `npm install`
- `npm run dev`

## Backend
- `cd backend`
- `python -m venv .venv`
- `.venv\\Scripts\\activate` (Windows)
- `pip install -r requirements.txt`
- `uvicorn app.main:app --reload`
